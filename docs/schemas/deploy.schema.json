{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Deploy Entry",
  "description": "Release patterns, rollback anxiety, and deployment language that sounds routine until the hidden dependency logs in.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "category",
    "title",
    "summary",
    "tags",
    "tone",
    "version",
    "created_at",
    "updated_at",
    "release_type",
    "rollback_risk",
    "hidden_dependency",
    "operator_note",
    "containment"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable slug for the entry."
    },
    "category": {
      "type": "string",
      "description": "Category slug used in API paths."
    },
    "title": {
      "type": "string",
      "description": "Display title."
    },
    "summary": {
      "type": "string",
      "description": "Short, high-signal description."
    },
    "tags": {
      "type": "array",
      "description": "Searchable tags for browsing and filtering.",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "tone": {
      "type": "string",
      "description": "Primary writing mode."
    },
    "version": {
      "type": "string",
      "description": "Content object schema version."
    },
    "created_at": {
      "type": "string",
      "description": "Initial publication date in ISO local date form."
    },
    "updated_at": {
      "type": "string",
      "description": "Most recent update date in ISO local date form."
    },
    "release_type": {
      "type": "string",
      "description": "Kind of deployment or rollout."
    },
    "rollback_risk": {
      "type": "string",
      "description": "How ugly rollback is likely to get.",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ]
    },
    "hidden_dependency": {
      "type": "string",
      "description": "Dependency that turns routine into special."
    },
    "operator_note": {
      "type": "string",
      "description": "What the release operator would say privately."
    },
    "containment": {
      "type": "string",
      "description": "Move that keeps the blast radius bounded."
    }
  }
}
