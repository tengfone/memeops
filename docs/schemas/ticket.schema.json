{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Ticket Entry",
  "description": "Backlog requests for work that arrived disguised as a small ask and quietly unfolded into cross-team archaeology.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "category",
    "title",
    "summary",
    "tags",
    "tone",
    "version",
    "created_at",
    "updated_at",
    "request",
    "hidden_work",
    "priority",
    "truthful_status",
    "expected_response"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable slug for the entry."
    },
    "category": {
      "type": "string",
      "description": "Category slug used in API paths."
    },
    "title": {
      "type": "string",
      "description": "Display title."
    },
    "summary": {
      "type": "string",
      "description": "Short, high-signal description."
    },
    "tags": {
      "type": "array",
      "description": "Searchable tags for browsing and filtering.",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "tone": {
      "type": "string",
      "description": "Primary writing mode."
    },
    "version": {
      "type": "string",
      "description": "Content object schema version."
    },
    "created_at": {
      "type": "string",
      "description": "Initial publication date in ISO local date form."
    },
    "updated_at": {
      "type": "string",
      "description": "Most recent update date in ISO local date form."
    },
    "request": {
      "type": "string",
      "description": "Thing the ticket appears to ask for."
    },
    "hidden_work": {
      "type": "string",
      "description": "Real effort concealed by the description."
    },
    "priority": {
      "type": "string",
      "description": "Practical urgency level.",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ]
    },
    "truthful_status": {
      "type": "string",
      "description": "What the ticket state should really say."
    },
    "expected_response": {
      "type": "string",
      "description": "Most reasonable answer from the owner."
    }
  }
}
