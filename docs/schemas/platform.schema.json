{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Platform Entry",
  "description": "Internal platform offerings, adoption theater, and the steady translation of one team’s preference into everyone else’s workflow.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "category",
    "title",
    "summary",
    "tags",
    "tone",
    "version",
    "created_at",
    "updated_at",
    "offering",
    "actual_job",
    "adoption_risk",
    "team_cost",
    "retirement_plan"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable slug for the entry."
    },
    "category": {
      "type": "string",
      "description": "Category slug used in API paths."
    },
    "title": {
      "type": "string",
      "description": "Display title."
    },
    "summary": {
      "type": "string",
      "description": "Short, high-signal description."
    },
    "tags": {
      "type": "array",
      "description": "Searchable tags for browsing and filtering.",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "tone": {
      "type": "string",
      "description": "Primary writing mode."
    },
    "version": {
      "type": "string",
      "description": "Content object schema version."
    },
    "created_at": {
      "type": "string",
      "description": "Initial publication date in ISO local date form."
    },
    "updated_at": {
      "type": "string",
      "description": "Most recent update date in ISO local date form."
    },
    "offering": {
      "type": "string",
      "description": "Named platform capability."
    },
    "actual_job": {
      "type": "string",
      "description": "What the platform is really doing."
    },
    "adoption_risk": {
      "type": "string",
      "description": "Likelihood of forced or performative adoption.",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ]
    },
    "team_cost": {
      "type": "string",
      "description": "Cost paid by the team operating it."
    },
    "retirement_plan": {
      "type": "string",
      "description": "How this should eventually be dismantled or absorbed."
    }
  }
}
