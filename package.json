{
  "name": "memeops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A static MemeOps API for infra humor, AI agent satire, and postmortem-grade truth.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate:content": "node scripts/generate-content.ts",
    "build": "node scripts/build-content.ts",
    "validate": "node scripts/validate-content.ts",
    "generate:indexes": "node scripts/generate-indexes.ts",
    "sync:docs": "node scripts/sync-docs.ts",
    "preview": "python3 -m http.server 4173 -d docs"
  }
}
